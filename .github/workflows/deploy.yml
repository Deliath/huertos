name: Desplegar en GitHub Pages

on:
  push:
    branches: [main]
  # Permite volver a publicar a mano desde la pestaña Actions.
  workflow_dispatch:

# Permisos mínimos: leer el código y publicar en Pages, nada más.
permissions:
  contents: read
  pages: write
  id-token: write

# Dos push seguidos no deben pisarse. No se cancela el que esté en curso para
# no dejar un despliegue a medias.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  verificar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Instalar dependencias
        run: npm ci

      - name: Pasar el linter
        run: npm run lint

      # npm test por sí solo no basta: si Vitest no consigue arrancar un
      # proceso de trabajo, el archivo afectado no se ejecuta y el resumen no
      # lo distingue de los que sí pasaron. verificar-suite.sh compara el
      # número de archivos ejecutados con los que hay en disco.
      - name: Ejecutar los tests y verificar que la suite se ejecutó entera
        run: bash scripts/verificar-suite.sh 1

      - name: Compilar
        run: npm run build

      - uses: actions/configure-pages@v5

      - name: Subir dist/ como artefacto de Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  desplegar:
    needs: verificar
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.despliegue.outputs.page_url }}
    steps:
      - name: Publicar
        id: despliegue
        uses: actions/deploy-pages@v4
